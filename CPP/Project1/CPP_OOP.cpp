#include <iostream>
#include <algorithm>
#include <vector>
#include <string>

typedef long long ll ;

#define pi pair <int ,int > 
#define vi vector <int > 
#define fr(i,a,b) for(int i=a ; i < b ; i++ )
#define testi(ok) if(ok==true) cout << "YES"; else cout << "NO"; 

using namespace std;

int main  ()
{
    int n,m,a,b;
    bool ok=false;
    a=b=10;
    if (a == b ==10)
    {
        cout << "mafhemet chy" << endl ;
    }
    
    cout << a << "  w hedhi b " << b << endl;
    return 0;
}